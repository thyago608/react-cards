import styled from "styled-components";

export const Container = styled.main`
  form {
    margin-top: 8rem;

    display: flex;
    flex-direction: column;
    align-items: center;

    gap: 2rem;
  }
`;
